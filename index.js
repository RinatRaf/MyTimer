/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {ObservableApp} from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ObservableApp);
